# Dissertation ML Evidence Register

The following artefacts must be generated by **running the implemented code on
real data / a real Pi**. They are intentionally absent — none are fabricated.

| Artefact | Produced by | Output location | Prerequisite |
|---|---|---|---|
| EDA charts (class balance, bbox/image sizes, brightness) | `notebooks/01_dataset_eda.ipynb` / `src.data.generate_statistics` | `ml/outputs/eda/` | Verified RGB dataset present |
| Training curves (loss, mAP) | `notebooks/02_train_yolo.ipynb` / `src.training.train_yolo` | `ml/outputs/runs/.../` | Dataset + GPU |
| Test metrics (P/R/F1, mAP50, mAP50-95, per-class) | `src.evaluation.evaluate_detection` | `ml/outputs/evaluation/metrics_test.json` | Trained weights |
| Confusion matrix, PR curves | Ultralytics `val()` plots | `ml/outputs/runs/.../` | Trained weights |
| Prediction sample grid | `src.deployment.inference` on test frames | `ml/outputs/` | Trained weights |
| Raspberry Pi latency/FPS benchmark | `src.deployment.benchmark` (run **on the Pi**) | record manually | Exported model on Pi |
| EfficientNet baseline metrics (optional) | `notebooks/03_efficientnet_baseline.ipynb` | record manually | Verified bird crops |

Keep validation, test, Pi-benchmark and dev-smoke-test results clearly separated
when reporting. Record the final trained model name + Ultralytics version in
`ml/models/MODEL_CARD.md` after training.
"""
